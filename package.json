{
    "name": "log-filter",
    "version": "0.0.1",
    "description": "",
    "main": "dist/index.js",
    "scripts": {
        "build": "yarn run build:clean && yarn run build:tsc:prod",
        "build:dev": "yarn run build:clean && yarn run build:tsc:dev",
        "build:clean": "rimraf dist",
        "build:tsc:prod": "tsc --project tsconfig.json",
        "build:tsc:dev": "tsc --project tsconfig.dev.json",
        "test": "yarn run build:dev && jest dist/*.test.js",
        "test:lint": "eslint . --ext .ts"
    },
    "author": "",
    "license": "MIT",
    "prettier": {
        "tabWidth": 4
    },
    "devDependencies": {
        "@jest/globals": "^26.6.2",
        "@typescript-eslint/eslint-plugin": "^4.9.0",
        "@typescript-eslint/parser": "^4.9.0",
        "eslint": "^7.14.0",
        "jest": "^26.6.2",
        "rimraf": "^3.0.2",
        "typescript": "^4.1.2"
    },
    "dependencies": {
        "arg": "^5.0.0"
    }
}
