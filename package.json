{
    "name": "log-filter",
    "version": "0.0.1",
    "description": "",
    "main": "dist/index.js",
    "scripts": {
        "build": "yarn run build:clean && yarn run build:tsc",
        "build:clean": "rimraf dist",
        "build:tsc": "tsc",
        "test": "yarn run build && jest dist/*.test.js"
    },
    "author": "",
    "license": "MIT",
    "prettier": {
        "tabWidth": 4
    },
    "devDependencies": {
        "@jest/globals": "^26.6.2",
        "jest": "^26.6.2",
        "rimraf": "^3.0.2",
        "typescript": "^4.1.2"
    },
    "dependencies": {}
}
